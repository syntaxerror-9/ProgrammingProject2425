package it.unibz.inf.pp.clash.model.boardchecker;

import it.unibz.inf.pp.clash.model.snapshot.Snapshot;

// Responsible for checking the board for combinations of units.
public class BoardChecker {
    public static void processAttacks(Snapshot snapshot) {

    }
}
